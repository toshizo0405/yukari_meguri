<div class="background_sign">
 <div class="container">
   <br>
   <div class="row">
    <div class="col-4"></div>
    <div class="col-4">
      <h2>新規会員登録</h2>
      <p>新規の方はこちらへ<br>入力をお願いします。</p>
    </div>
   </div>
   <div class="row">
    <div class="col-4"></div>
    <div class="col-4">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "public/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.label :"メールアドレス" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.label :"名前(性)" %><br>
          <%= f.text_field :last_name %>
          <%= f.label :"(名)" %><br>
          <%= f.text_field :first_name %>
        </div>

        <div class="field">
          <%= f.label :"フリガナ(セイ)" %><br>
          <%= f.text_field :last_name_kana %>
          <%= f.label :"(メイ)" %><br>
          <%= f.text_field :first_name_kana %>
        </div>

        <div class="field">
          <%= f.label :"パスワード" %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :"パスワード'(確認)" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
        </div>
        <br>
        <div class="row actions">
         <div class="col-4"></div>
         <div class="col-4">
            <%= f.submit "新規登録",class: 'sign-btn' %>
         </div>
        </div>
      <% end %>
    </div>
   </div>
   <br>
   <div class="row">
     <div class="col-4"></div>
     <div class="col-6">
      <h4>すでに登録している会員の方は</h4>
      <h4>こちらから<%= render "public/shared/links" %>してください。</h4>
     </div>
   </div>

 </div>
</div>
