<h4>ようこそ!ゆかり巡りの世界へ!</h4>

<h3><%= link_to '投稿はこちらへ',new_post_path %></h3>


<h1>投稿一覧(全<%= Post.count %>件）</h1>

<%= search_form_for @search do |f| %>
  # nameカラムに対して部分一致検索ができる
  <%= f.label :posts, "商品名を含む" %>
  <%= f.search_field :title %>
  <%= f.search_field :tag %>
  # 検索ボタン
  <div class="actions"><%= f.submit "検索" %></div>
<% end %>

<table border="1" >
  <thead>
    <tr>
      <th>投稿日</th>
      <th>投稿タイトル</th>
    </tr>
  </thead>
  <% @posts.each do |input| %>
  <tbody>
    <tr>
      <td><%= input.created_at.strftime('%Y/%m/%d') %></td>
      <td><%= link_to input.title,post_path(input.id) %></td>
    </tr>
  </tbody>
  <% end %>
</table>
<%= paginate @posts %>